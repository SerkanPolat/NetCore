@model NetCore.Models.UserLoginModel

@{
    ViewData["Title"] = "View";
}

<h1>View</h1>

<h4>UserLoginModel</h4>
<link href="~/content/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet" />
<script src="~/content/jquery/dist/jquery.min.js"></script>
<hr />
<div class="row">
    <div class="col-md-4">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="userName" class="control-label"></label>
            <input asp-for="userName" id="username" class="form-control" />
            <span asp-validation-for="UserName" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="password" class="control-label"></label>
            <input asp-for="password" id="password" class="form-control" value="asdawd" />
            <span asp-validation-for="password" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="rememberMe" class="control-label">Hatirla Beni</label>
            <input type="checkbox" asp-for="rememberMe" id="rememberMe" />
            <span asp-validation-for="rememberMe" class="text-danger"></span>
        </div>
        <div class="form-group">
            <button onclick="gonder()" value="GONDER">Gonder</button>
        </div>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
<script>

    var gonder = function () {
        console.log("merhaba");
        var param = {
            "userName": $('#username').val(),
            "password": $('#password').val(),
            "rememberMe": $('#rememberMe').is(':checked')
        }

        $.ajax({
            type: "POST",
            url: "https://localhost:44307/Home/Login",
            data: param,
            success: function (result) {
                console.log(result);
            }
        });
    }
</script>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
